import type { NextPage } from "next";

// Import components
import MintPage from "../../components/AboutPage/AboutPage";

const Home: NextPage = () => {
    return <MintPage />;
};

export default Home;
