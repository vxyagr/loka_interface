import type { NextPage } from "next";

// Import components
import MintPage from "../../components/MintPage/MintPage";

const Home: NextPage = () => {
    return <MintPage />;
};

export default Home;
